
var edited_html;
var edited_id;
// var policyList = $('#policy_list');
var serviceLink = $('#new_service_link');
shuffleLinks(serviceLink);
$('#service_list li:last-child').append('<%= j render(@service) %>');

edited_id = $('#service_list li:last').attr('id');
edited_html = $('#service_list li:last').html();
$('li#' + edited_id + ':first').replaceWith(edited_html);
$('#service_list li:last').remove();

